rules:

  unpinned-uses:
    config:
      policies:
        actions/*: hash-pin
        github/*: hash-pin
        softprops/*: hash-pin
        astral-sh/*: hash-pin
  
  forbidden-uses:
    config:
      allow:
        - actions/checkout
        - actions/cache
        - actions/setup-python
        - github/codeql-action/upload-sarif
        - softprops/action-gh-release
        - astral-sh/setup-uv
